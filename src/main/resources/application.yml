server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/seniorlink?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: seniorlink
    password: seniorlink2024!
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: update
      show_sql: true
      format_sql: true
      dialect: org.hibernate.dialect.MySQLDialect
    properties:
      jakarta:
        persistence:
          sharedCache:
            mode: ENABLE_SELECTIVE